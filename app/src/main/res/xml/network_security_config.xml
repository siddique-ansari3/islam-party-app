<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext HTTP is only permitted to the local dev backend (Android emulator alias).
     Production deployments must use HTTPS - update API_BASE_URL to an https:// URL and
     this config will enforce TLS for all other hosts. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
